<%= form_for @trip do |f| %>
  <%= f.label :give_your_trip_a_name %>
  <%= f.text_field :name %><br>
  <%= f.collection_select(:user_id, User.all, :id, :name) %>

  <%= f.submit "Submit" %><br>

  <%end%>
